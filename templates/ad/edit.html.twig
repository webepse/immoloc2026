{% extends 'base.html.twig' %}

{% block title %}Modification d'une annonce{% endblock %}

{% form_theme myForm 'ad/_collection.html.twig' %}

{% block body %}
    <div class="container full">
        <h2 class="mb-3">Modification de l'annonce: {{ad.title}}</h2>
        {{ form_start(myForm) }}

        {{ form_widget(myForm) }}

        <button type="submit" class="btn btn-warning mt-3">Modification</button>

        {{ form_end(myForm) }}
    </div>
{% endblock %}


{% block javascripts %}
    {{ encore_entry_script_tags('annonce') }}
{% endblock %}

